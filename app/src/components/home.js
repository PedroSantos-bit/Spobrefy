/*import React from 'react';
import { View, Text, StyleSheet, Image, TouchableOpacity } from 'react-native';
import { Ionicons, Entypo, Feather } from '@expo/vector-icons';
import { LinearGradient } from 'expo-linear-gradient';

const HomeScreen = () => {
  return (
    <LinearGradient colors={['#fff', '#fff']} style={styles.container}>
      {/* Avatar 
      <Image
        source={{ uri: 'https://i.pravatar.cc/100' }} // imagem de avatar
        style={styles.avatar}
      />

      {/* Título *
      <Text style={styles.title}>Home</Text>

      {/* Capa do Álbum *
      <View style={styles.albumContainer}>
        <Image
          source={{ uri: 'https://i.imgur.com/OfyXWkJ.jpg' }} // capa exemplo
          style={styles.albumImage}
        />
      </View>

      {/* Informações da música *
      <Text style={styles.songTitle}>Julie</Text>
      <Text style={styles.albumTitle}>Flutter — Album</Text>

      {/* Barra de progresso *
      <View style={styles.progressBarContainer}>
        <View style={styles.progressBar}>
          <View style={styles.progressFill} />
        </View>
      </View>

      {/* Controles *
      <View style={styles.controls}>
        <TouchableOpacity>
          <Ionicons name="shuffle" size={24} color="black" />
        </TouchableOpacity>
        <TouchableOpacity>
          <Ionicons name="play-skip-back" size={28} color="black" />
        </TouchableOpacity>
        <TouchableOpacity style={styles.playButton}>
          <Ionicons name="play" size={28} color="black" />
        </TouchableOpacity>
        <TouchableOpacity>
          <Ionicons name="play-skip-forward" size={28} color="black" />
        </TouchableOpacity>
        <TouchableOpacity>
          <Feather name="repeat" size={24} color="black" />
        </TouchableOpacity>
      </View>

      {/* Navegação inferior *
      <View style={styles.navbar}>
        <TouchableOpacity style={styles.navItem}>
          <Entypo name="home" size={24} color="#00d4ff" />
          <Text style={styles.navLabel}>home</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.navItem}>
          <Feather name="search" size={24} color="white" />
          <Text style={styles.navLabel}>search</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.navItem}>
          <Feather name="heart" size={24} color="white" />
          <Text style={styles.navLabel}>favorite</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.navItem}>
          <Feather name="user" size={24} color="white" />
          <Text style={styles.navLabel}>profile</Text>
        </TouchableOpacity>
      </View>
    </LinearGradient>
  );
};

export default HomeScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 50,
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  avatar: {
    width: 50,
    height: 50,
    borderRadius: 25,
    marginBottom: 10,
  },
  title: {
    fontSize: 24,
    marginBottom: 20,
    fontWeight: 'bold',
  },
  albumContainer: {
    backgroundColor: '#ccc',
    padding: 10,
    borderRadius: 10,
    marginBottom: 20,
  },
  albumImage: {
    width: 200,
    height: 200,
    resizeMode: 'cover',
  },
  songTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    marginTop: 10,
  },
  albumTitle: {
    fontSize: 16,
    color: '#555',
    marginBottom: 20,
  },
  progressBarContainer: {
    width: '80%',
    height: 20,
    marginBottom: 20,
  },
  progressBar: {
    height: 4,
    backgroundColor: '#ccc',
    borderRadius: 2,
  },
  progressFill: {
    width: '60%', // simula progresso
    height: 4,
    backgroundColor: '#00e0d6',
    borderRadius: 2,
  },
  controls: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: '80%',
    alignItems: 'center',
    marginBottom: 30,
  },
  playButton: {
    backgroundColor: 'white',
    borderRadius: 30,
    padding: 10,
    elevation: 3,
  },
  navbar: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    width: '100%',
    paddingVertical: 10,
    backgroundColor: 'black',
  },
  navItem: {
    alignItems: 'center',
  },
  navLabel: {
    fontSize: 12,
    color: 'white',
  },
});*/ 
